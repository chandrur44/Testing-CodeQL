# Intentional bait image. Built only to verify the Trivy image scanner
# is reporting CVEs under the "Trivy Image Scanner" tool label in the
# Security tab. NOT used by the application.

FROM alpine:3.10
